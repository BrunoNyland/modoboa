# Dependencies
node_modules
.npm

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build output (also produced at ../modoboa/frontend_dist)
dist
dist-ssr
/static/dist

# Vite / Module Federation caches and temp
.vite
node_modules/.vite
.__mf__temp/
webpack-stats.json

# Storybook build output
storybook-static

# Test coverage (vitest)
coverage
*.lcov

# Local env overrides (keep committed .env, ignore local variants)
.env.local
.env.*.local

# Generated translations (committed source lives in src/locale/translations.json)
src/translations.json

# Network captures (HAR) — may contain tokens/cookies; never commit
*.har

# Project uses yarn; do not commit the npm lockfile
package-lock.json

# Editor / OS noise
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
